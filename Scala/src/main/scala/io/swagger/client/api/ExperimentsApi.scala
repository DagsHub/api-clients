/**
 * DagsHub API
 * This API provides a way to retrive & interact with data about DagsHub repositories, users, issues, webhooks and more. The API is also used by the [DagsHub Direct Data Access streaming client](https://github.com/DagsHub/client) to stream content from a repository for easier and faster training. 
 *
 * OpenAPI spec version: 1.0.2
 * 
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 * Do not edit the class manually.
 */
package io.swagger.client.api

import java.text.SimpleDateFormat

import io.swagger.client.model.EditExperimentPost
import io.swagger.client.model.ExperimentKeys
import io.swagger.client.{ApiInvoker, ApiException}

import com.sun.jersey.multipart.FormDataMultiPart
import com.sun.jersey.multipart.file.FileDataBodyPart

import javax.ws.rs.core.MediaType

import java.io.File
import java.util.Date
import java.util.TimeZone

import scala.collection.mutable.HashMap

import com.wordnik.swagger.client._
import scala.concurrent.Future
import collection.mutable

import java.net.URI

import com.wordnik.swagger.client.ClientResponseReaders.Json4sFormatsReader._
import com.wordnik.swagger.client.RequestWriters.Json4sFormatsWriter._

import scala.concurrent.ExecutionContext.Implicits.global
import scala.concurrent._
import scala.concurrent.duration._
import scala.util.{Failure, Success, Try}

import org.json4s._

class ExperimentsApi(
  val defBasePath: String = "https://dagshub.com/api/v1",
  defApiInvoker: ApiInvoker = ApiInvoker
) {
  private lazy val dateTimeFormatter = {
    val formatter = new SimpleDateFormat("yyyy-MM-dd'T'HH:mm:ss.SSSZ")
    formatter.setTimeZone(TimeZone.getTimeZone("UTC"))
    formatter
  }
  private val dateFormatter = {
    val formatter = new SimpleDateFormat("yyyy-MM-dd")
    formatter.setTimeZone(TimeZone.getTimeZone("UTC"))
    formatter
  }
  implicit val formats = new org.json4s.DefaultFormats {
    override def dateFormatter = dateTimeFormatter
  }
  implicit val stringReader: ClientResponseReader[String] = ClientResponseReaders.StringReader
  implicit val unitReader: ClientResponseReader[Unit] = ClientResponseReaders.UnitReader
  implicit val jvalueReader: ClientResponseReader[JValue] = ClientResponseReaders.JValueReader
  implicit val jsonReader: ClientResponseReader[Nothing] = JsonFormatsReader
  implicit val stringWriter: RequestWriter[String] = RequestWriters.StringWriter
  implicit val jsonWriter: RequestWriter[Nothing] = JsonFormatsWriter

  var basePath: String = defBasePath
  var apiInvoker: ApiInvoker = defApiInvoker

  def addHeader(key: String, value: String): mutable.HashMap[String, String] = {
    apiInvoker.defaultHeaders += key -> value
  }

  val config: SwaggerConfig = SwaggerConfig.forUrl(new URI(defBasePath))
  val client = new RestClient(config)
  val helper = new ExperimentsApiAsyncHelper(client, config)

  /**
   * Add label to experiment
   * 
   *
   * @param owner owner of the repository 
   * @param repo name of the repository 
   * @param experimentKey a valid experiment key 
   * @param experimentLabel A label for the experiment, max 128 characters. 
   * @return void
   */
  def addExperimentLabel(owner: String, repo: String, experimentKey: String, experimentLabel: String) = {
    val await = Try(Await.result(addExperimentLabelAsync(owner, repo, experimentKey, experimentLabel), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Add label to experiment asynchronously
   * 
   *
   * @param owner owner of the repository 
   * @param repo name of the repository 
   * @param experimentKey a valid experiment key 
   * @param experimentLabel A label for the experiment, max 128 characters. 
   * @return Future(void)
   */
  def addExperimentLabelAsync(owner: String, repo: String, experimentKey: String, experimentLabel: String) = {
      helper.addExperimentLabel(owner, repo, experimentKey, experimentLabel)
  }

  /**
   * Delete experiment label
   * 
   *
   * @param owner owner of the repository 
   * @param repo name of the repository 
   * @param experimentKey a valid experiment key 
   * @param experimentLabel A label for the experiment, max 128 characters. 
   * @return void
   */
  def deleteExperimentLabel(owner: String, repo: String, experimentKey: String, experimentLabel: String) = {
    val await = Try(Await.result(deleteExperimentLabelAsync(owner, repo, experimentKey, experimentLabel), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Delete experiment label asynchronously
   * 
   *
   * @param owner owner of the repository 
   * @param repo name of the repository 
   * @param experimentKey a valid experiment key 
   * @param experimentLabel A label for the experiment, max 128 characters. 
   * @return Future(void)
   */
  def deleteExperimentLabelAsync(owner: String, repo: String, experimentKey: String, experimentLabel: String) = {
      helper.deleteExperimentLabel(owner, repo, experimentKey, experimentLabel)
  }

  /**
   * Edit experiment info
   * Edit experiment info, currently supports just changing experiment name.
   *
   * @param owner owner of the repository 
   * @param repo name of the repository 
   * @param experimentKey a valid experiment key 
   * @param body  (optional)
   * @return void
   */
  def editExperiment(owner: String, repo: String, experimentKey: String, body: Option[EditExperimentPost] = None) = {
    val await = Try(Await.result(editExperimentAsync(owner, repo, experimentKey, body), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Edit experiment info asynchronously
   * Edit experiment info, currently supports just changing experiment name.
   *
   * @param owner owner of the repository 
   * @param repo name of the repository 
   * @param experimentKey a valid experiment key 
   * @param body  (optional)
   * @return Future(void)
   */
  def editExperimentAsync(owner: String, repo: String, experimentKey: String, body: Option[EditExperimentPost] = None) = {
      helper.editExperiment(owner, repo, experimentKey, body)
  }

  /**
   * Get experiment metrics for experiment
   * 
   *
   * @param owner owner of the repository 
   * @param repo name of the repository 
   * @param body  (optional)
   * @return void
   */
  def getExperimentMetrics(owner: String, repo: String, body: Option[ExperimentKeys] = None) = {
    val await = Try(Await.result(getExperimentMetricsAsync(owner, repo, body), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Get experiment metrics for experiment asynchronously
   * 
   *
   * @param owner owner of the repository 
   * @param repo name of the repository 
   * @param body  (optional)
   * @return Future(void)
   */
  def getExperimentMetricsAsync(owner: String, repo: String, body: Option[ExperimentKeys] = None) = {
      helper.getExperimentMetrics(owner, repo, body)
  }

  /**
   * List Experiments
   * Get a list of experiments in the given repository
   *
   * @param owner owner of the repository 
   * @param repo name of the repository 
   * @return void
   */
  def getExperiments(owner: String, repo: String) = {
    val await = Try(Await.result(getExperimentsAsync(owner, repo), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * List Experiments asynchronously
   * Get a list of experiments in the given repository
   *
   * @param owner owner of the repository 
   * @param repo name of the repository 
   * @return Future(void)
   */
  def getExperimentsAsync(owner: String, repo: String) = {
      helper.getExperiments(owner, repo)
  }

}

class ExperimentsApiAsyncHelper(client: TransportClient, config: SwaggerConfig) extends ApiClient(client, config) {

  def addExperimentLabel(owner: String,
    repo: String,
    experimentKey: String,
    experimentLabel: String)(implicit reader: ClientResponseReader[Unit]): Future[Unit] = {
    // create path and map variables
    val path = (addFmt("/repos/{owner}/{repo}/experiments/experiment/{experimentKey}/labels/{experimentLabel}")
      replaceAll("\\{" + "owner" + "\\}", owner.toString)
      replaceAll("\\{" + "repo" + "\\}", repo.toString)
      replaceAll("\\{" + "experimentKey" + "\\}", experimentKey.toString)
      replaceAll("\\{" + "experimentLabel" + "\\}", experimentLabel.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (owner == null) throw new Exception("Missing required parameter 'owner' when calling ExperimentsApi->addExperimentLabel")

    if (repo == null) throw new Exception("Missing required parameter 'repo' when calling ExperimentsApi->addExperimentLabel")

    if (experimentKey == null) throw new Exception("Missing required parameter 'experimentKey' when calling ExperimentsApi->addExperimentLabel")

    if (experimentLabel == null) throw new Exception("Missing required parameter 'experimentLabel' when calling ExperimentsApi->addExperimentLabel")


    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def deleteExperimentLabel(owner: String,
    repo: String,
    experimentKey: String,
    experimentLabel: String)(implicit reader: ClientResponseReader[Unit]): Future[Unit] = {
    // create path and map variables
    val path = (addFmt("/repos/{owner}/{repo}/experiments/experiment/{experimentKey}/labels/{experimentLabel}")
      replaceAll("\\{" + "owner" + "\\}", owner.toString)
      replaceAll("\\{" + "repo" + "\\}", repo.toString)
      replaceAll("\\{" + "experimentKey" + "\\}", experimentKey.toString)
      replaceAll("\\{" + "experimentLabel" + "\\}", experimentLabel.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (owner == null) throw new Exception("Missing required parameter 'owner' when calling ExperimentsApi->deleteExperimentLabel")

    if (repo == null) throw new Exception("Missing required parameter 'repo' when calling ExperimentsApi->deleteExperimentLabel")

    if (experimentKey == null) throw new Exception("Missing required parameter 'experimentKey' when calling ExperimentsApi->deleteExperimentLabel")

    if (experimentLabel == null) throw new Exception("Missing required parameter 'experimentLabel' when calling ExperimentsApi->deleteExperimentLabel")


    val resFuture = client.submit("DELETE", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def editExperiment(owner: String,
    repo: String,
    experimentKey: String,
    body: Option[EditExperimentPost] = None
    )(implicit reader: ClientResponseReader[Unit], writer: RequestWriter[Option[EditExperimentPost]]): Future[Unit] = {
    // create path and map variables
    val path = (addFmt("/repos/{owner}/{repo}/experiments/experiment/{experimentKey}")
      replaceAll("\\{" + "owner" + "\\}", owner.toString)
      replaceAll("\\{" + "repo" + "\\}", repo.toString)
      replaceAll("\\{" + "experimentKey" + "\\}", experimentKey.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (owner == null) throw new Exception("Missing required parameter 'owner' when calling ExperimentsApi->editExperiment")

    if (repo == null) throw new Exception("Missing required parameter 'repo' when calling ExperimentsApi->editExperiment")

    if (experimentKey == null) throw new Exception("Missing required parameter 'experimentKey' when calling ExperimentsApi->editExperiment")


    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getExperimentMetrics(owner: String,
    repo: String,
    body: Option[ExperimentKeys] = None
    )(implicit reader: ClientResponseReader[Unit], writer: RequestWriter[Option[ExperimentKeys]]): Future[Unit] = {
    // create path and map variables
    val path = (addFmt("/repos/{owner}/{repo}/experiments/metricsForExperiments")
      replaceAll("\\{" + "owner" + "\\}", owner.toString)
      replaceAll("\\{" + "repo" + "\\}", repo.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (owner == null) throw new Exception("Missing required parameter 'owner' when calling ExperimentsApi->getExperimentMetrics")

    if (repo == null) throw new Exception("Missing required parameter 'repo' when calling ExperimentsApi->getExperimentMetrics")


    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getExperiments(owner: String,
    repo: String)(implicit reader: ClientResponseReader[Unit]): Future[Unit] = {
    // create path and map variables
    val path = (addFmt("/repos/{owner}/{repo}/experiments")
      replaceAll("\\{" + "owner" + "\\}", owner.toString)
      replaceAll("\\{" + "repo" + "\\}", repo.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (owner == null) throw new Exception("Missing required parameter 'owner' when calling ExperimentsApi->getExperiments")

    if (repo == null) throw new Exception("Missing required parameter 'repo' when calling ExperimentsApi->getExperiments")


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }


}
