/**
 * DagsHub API
 * This API provides a way to retrive & interact with data about DagsHub repositories, users, issues, webhooks and more. The API is also used by the [DagsHub Direct Data Access streaming client](https://github.com/DagsHub/client) to stream content from a repository for easier and faster training. 
 *
 * OpenAPI spec version: 1.0.3
 * 
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 * Do not edit the class manually.
 */
package io.swagger.client.api

import java.text.SimpleDateFormat

import io.swagger.client.model.GrantLabelStudioAccess
import io.swagger.client.model.LabelStudioProjectAccess
import io.swagger.client.model.inline_response_200
import io.swagger.client.{ApiInvoker, ApiException}

import com.sun.jersey.multipart.FormDataMultiPart
import com.sun.jersey.multipart.file.FileDataBodyPart

import javax.ws.rs.core.MediaType

import java.io.File
import java.util.Date
import java.util.TimeZone

import scala.collection.mutable.HashMap

import com.wordnik.swagger.client._
import scala.concurrent.Future
import collection.mutable

import java.net.URI

import com.wordnik.swagger.client.ClientResponseReaders.Json4sFormatsReader._
import com.wordnik.swagger.client.RequestWriters.Json4sFormatsWriter._

import scala.concurrent.ExecutionContext.Implicits.global
import scala.concurrent._
import scala.concurrent.duration._
import scala.util.{Failure, Success, Try}

import org.json4s._

class AnnotationsApi(
  val defBasePath: String = "https://dagshub.com/api/v1",
  defApiInvoker: ApiInvoker = ApiInvoker
) {
  private lazy val dateTimeFormatter = {
    val formatter = new SimpleDateFormat("yyyy-MM-dd'T'HH:mm:ss.SSSZ")
    formatter.setTimeZone(TimeZone.getTimeZone("UTC"))
    formatter
  }
  private val dateFormatter = {
    val formatter = new SimpleDateFormat("yyyy-MM-dd")
    formatter.setTimeZone(TimeZone.getTimeZone("UTC"))
    formatter
  }
  implicit val formats = new org.json4s.DefaultFormats {
    override def dateFormatter = dateTimeFormatter
  }
  implicit val stringReader: ClientResponseReader[String] = ClientResponseReaders.StringReader
  implicit val unitReader: ClientResponseReader[Unit] = ClientResponseReaders.UnitReader
  implicit val jvalueReader: ClientResponseReader[JValue] = ClientResponseReaders.JValueReader
  implicit val jsonReader: ClientResponseReader[Nothing] = JsonFormatsReader
  implicit val stringWriter: RequestWriter[String] = RequestWriters.StringWriter
  implicit val jsonWriter: RequestWriter[Nothing] = JsonFormatsWriter

  var basePath: String = defBasePath
  var apiInvoker: ApiInvoker = defApiInvoker

  def addHeader(key: String, value: String): mutable.HashMap[String, String] = {
    apiInvoker.defaultHeaders += key -> value
  }

  val config: SwaggerConfig = SwaggerConfig.forUrl(new URI(defBasePath))
  val client = new RestClient(config)
  val helper = new AnnotationsApiAsyncHelper(client, config)

  /**
   * Get team access level for annotation projects
   * Get the access level for a specific team and project (or all projects)
   *
   * @param owner owner of the repository 
   * @param repo name of the repository 
   * @param teamid Team ID 
   * @param projectId Label Studio project ID to filter by. If not specified, returns access for all projects. Use -1 for wildcard access. (optional)
   * @return LabelStudioProjectAccess
   */
  def getLabelStudioTeamAccess(owner: String, repo: String, teamid: Long, projectId: Option[Long] = None): Option[LabelStudioProjectAccess] = {
    val await = Try(Await.result(getLabelStudioTeamAccessAsync(owner, repo, teamid, projectId), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Get team access level for annotation projects asynchronously
   * Get the access level for a specific team and project (or all projects)
   *
   * @param owner owner of the repository 
   * @param repo name of the repository 
   * @param teamid Team ID 
   * @param projectId Label Studio project ID to filter by. If not specified, returns access for all projects. Use -1 for wildcard access. (optional)
   * @return Future(LabelStudioProjectAccess)
   */
  def getLabelStudioTeamAccessAsync(owner: String, repo: String, teamid: Long, projectId: Option[Long] = None): Future[LabelStudioProjectAccess] = {
      helper.getLabelStudioTeamAccess(owner, repo, teamid, projectId)
  }

  /**
   * Get user access level for annotation projects
   * Get the access level for a specific user and project (or all projects)
   *
   * @param owner owner of the repository 
   * @param repo name of the repository 
   * @param userid User ID 
   * @param projectId Label Studio project ID to filter by. If not specified, returns access for all projects. Use -1 for wildcard access. (optional)
   * @return inline_response_200
   */
  def getLabelStudioUserAccess(owner: String, repo: String, userid: Long, projectId: Option[Long] = None): Option[inline_response_200] = {
    val await = Try(Await.result(getLabelStudioUserAccessAsync(owner, repo, userid, projectId), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Get user access level for annotation projects asynchronously
   * Get the access level for a specific user and project (or all projects)
   *
   * @param owner owner of the repository 
   * @param repo name of the repository 
   * @param userid User ID 
   * @param projectId Label Studio project ID to filter by. If not specified, returns access for all projects. Use -1 for wildcard access. (optional)
   * @return Future(inline_response_200)
   */
  def getLabelStudioUserAccessAsync(owner: String, repo: String, userid: Long, projectId: Option[Long] = None): Future[inline_response_200] = {
      helper.getLabelStudioUserAccess(owner, repo, userid, projectId)
  }

  /**
   * Grant team access to annotation projects
   * Grant a team access to specific Label Studio annotation projects (organization repositories only)
   *
   * @param body  
   * @param owner owner of the repository 
   * @param repo name of the repository 
   * @param teamid Team ID 
   * @return void
   */
  def grantLabelStudioTeamAccess(body: GrantLabelStudioAccess, owner: String, repo: String, teamid: Long) = {
    val await = Try(Await.result(grantLabelStudioTeamAccessAsync(body, owner, repo, teamid), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Grant team access to annotation projects asynchronously
   * Grant a team access to specific Label Studio annotation projects (organization repositories only)
   *
   * @param body  
   * @param owner owner of the repository 
   * @param repo name of the repository 
   * @param teamid Team ID 
   * @return Future(void)
   */
  def grantLabelStudioTeamAccessAsync(body: GrantLabelStudioAccess, owner: String, repo: String, teamid: Long) = {
      helper.grantLabelStudioTeamAccess(body, owner, repo, teamid)
  }

  /**
   * Grant user access to annotation projects
   * Grant a user access to specific Label Studio annotation projects. User must be an organization member if the repo is owned by an organization.
   *
   * @param body  
   * @param owner owner of the repository 
   * @param repo name of the repository 
   * @param userid User ID 
   * @return void
   */
  def grantLabelStudioUserAccess(body: GrantLabelStudioAccess, owner: String, repo: String, userid: Long) = {
    val await = Try(Await.result(grantLabelStudioUserAccessAsync(body, owner, repo, userid), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Grant user access to annotation projects asynchronously
   * Grant a user access to specific Label Studio annotation projects. User must be an organization member if the repo is owned by an organization.
   *
   * @param body  
   * @param owner owner of the repository 
   * @param repo name of the repository 
   * @param userid User ID 
   * @return Future(void)
   */
  def grantLabelStudioUserAccessAsync(body: GrantLabelStudioAccess, owner: String, repo: String, userid: Long) = {
      helper.grantLabelStudioUserAccess(body, owner, repo, userid)
  }

  /**
   * List team access entries for annotation projects
   * Get a list of teams with access to Label Studio annotation projects in the repository (organization repositories only)
   *
   * @param owner owner of the repository 
   * @param repo name of the repository 
   * @param projectId Label Studio project ID to filter by. If not specified, returns access for all projects. Use -1 for wildcard access. (optional)
   * @return List[LabelStudioProjectAccess]
   */
  def listLabelStudioTeamAccess(owner: String, repo: String, projectId: Option[Long] = None): Option[List[LabelStudioProjectAccess]] = {
    val await = Try(Await.result(listLabelStudioTeamAccessAsync(owner, repo, projectId), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * List team access entries for annotation projects asynchronously
   * Get a list of teams with access to Label Studio annotation projects in the repository (organization repositories only)
   *
   * @param owner owner of the repository 
   * @param repo name of the repository 
   * @param projectId Label Studio project ID to filter by. If not specified, returns access for all projects. Use -1 for wildcard access. (optional)
   * @return Future(List[LabelStudioProjectAccess])
   */
  def listLabelStudioTeamAccessAsync(owner: String, repo: String, projectId: Option[Long] = None): Future[List[LabelStudioProjectAccess]] = {
      helper.listLabelStudioTeamAccess(owner, repo, projectId)
  }

  /**
   * List user access entries for annotation projects
   * Get a list of users with access to Label Studio annotation projects in the repository
   *
   * @param owner owner of the repository 
   * @param repo name of the repository 
   * @param projectId Label Studio project ID to filter by. If not specified, returns access for all projects. Use -1 for wildcard access. (optional)
   * @return List[LabelStudioProjectAccess]
   */
  def listLabelStudioUserAccess(owner: String, repo: String, projectId: Option[Long] = None): Option[List[LabelStudioProjectAccess]] = {
    val await = Try(Await.result(listLabelStudioUserAccessAsync(owner, repo, projectId), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * List user access entries for annotation projects asynchronously
   * Get a list of users with access to Label Studio annotation projects in the repository
   *
   * @param owner owner of the repository 
   * @param repo name of the repository 
   * @param projectId Label Studio project ID to filter by. If not specified, returns access for all projects. Use -1 for wildcard access. (optional)
   * @return Future(List[LabelStudioProjectAccess])
   */
  def listLabelStudioUserAccessAsync(owner: String, repo: String, projectId: Option[Long] = None): Future[List[LabelStudioProjectAccess]] = {
      helper.listLabelStudioUserAccess(owner, repo, projectId)
  }

  /**
   * Revoke team access to annotation projects
   * Revoke a team&#x27;s access to Label Studio annotation projects
   *
   * @param owner owner of the repository 
   * @param repo name of the repository 
   * @param teamid Team ID 
   * @param projectId Label Studio project ID to filter by. If not specified, returns access for all projects. Use -1 for wildcard access. (optional)
   * @return void
   */
  def revokeLabelStudioTeamAccess(owner: String, repo: String, teamid: Long, projectId: Option[Long] = None) = {
    val await = Try(Await.result(revokeLabelStudioTeamAccessAsync(owner, repo, teamid, projectId), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Revoke team access to annotation projects asynchronously
   * Revoke a team&#x27;s access to Label Studio annotation projects
   *
   * @param owner owner of the repository 
   * @param repo name of the repository 
   * @param teamid Team ID 
   * @param projectId Label Studio project ID to filter by. If not specified, returns access for all projects. Use -1 for wildcard access. (optional)
   * @return Future(void)
   */
  def revokeLabelStudioTeamAccessAsync(owner: String, repo: String, teamid: Long, projectId: Option[Long] = None) = {
      helper.revokeLabelStudioTeamAccess(owner, repo, teamid, projectId)
  }

  /**
   * Revoke user access to annotation projects
   * Revoke a user&#x27;s access to Label Studio annotation projects
   *
   * @param owner owner of the repository 
   * @param repo name of the repository 
   * @param userid User ID 
   * @param projectId Label Studio project ID to filter by. If not specified, returns access for all projects. Use -1 for wildcard access. (optional)
   * @return void
   */
  def revokeLabelStudioUserAccess(owner: String, repo: String, userid: Long, projectId: Option[Long] = None) = {
    val await = Try(Await.result(revokeLabelStudioUserAccessAsync(owner, repo, userid, projectId), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Revoke user access to annotation projects asynchronously
   * Revoke a user&#x27;s access to Label Studio annotation projects
   *
   * @param owner owner of the repository 
   * @param repo name of the repository 
   * @param userid User ID 
   * @param projectId Label Studio project ID to filter by. If not specified, returns access for all projects. Use -1 for wildcard access. (optional)
   * @return Future(void)
   */
  def revokeLabelStudioUserAccessAsync(owner: String, repo: String, userid: Long, projectId: Option[Long] = None) = {
      helper.revokeLabelStudioUserAccess(owner, repo, userid, projectId)
  }

}

class AnnotationsApiAsyncHelper(client: TransportClient, config: SwaggerConfig) extends ApiClient(client, config) {

  def getLabelStudioTeamAccess(owner: String,
    repo: String,
    teamid: Long,
    projectId: Option[Long] = None
    )(implicit reader: ClientResponseReader[LabelStudioProjectAccess]): Future[LabelStudioProjectAccess] = {
    // create path and map variables
    val path = (addFmt("/repos/{owner}/{repo}/annotations/access/teams/{teamid}")
      replaceAll("\\{" + "owner" + "\\}", owner.toString)
      replaceAll("\\{" + "repo" + "\\}", repo.toString)
      replaceAll("\\{" + "teamid" + "\\}", teamid.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (owner == null) throw new Exception("Missing required parameter 'owner' when calling AnnotationsApi->getLabelStudioTeamAccess")

    if (repo == null) throw new Exception("Missing required parameter 'repo' when calling AnnotationsApi->getLabelStudioTeamAccess")

    projectId match {
      case Some(param) => queryParams += "project_id" -> param.toString
      case _ => queryParams
    }

    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getLabelStudioUserAccess(owner: String,
    repo: String,
    userid: Long,
    projectId: Option[Long] = None
    )(implicit reader: ClientResponseReader[inline_response_200]): Future[inline_response_200] = {
    // create path and map variables
    val path = (addFmt("/repos/{owner}/{repo}/annotations/access/users/{userid}")
      replaceAll("\\{" + "owner" + "\\}", owner.toString)
      replaceAll("\\{" + "repo" + "\\}", repo.toString)
      replaceAll("\\{" + "userid" + "\\}", userid.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (owner == null) throw new Exception("Missing required parameter 'owner' when calling AnnotationsApi->getLabelStudioUserAccess")

    if (repo == null) throw new Exception("Missing required parameter 'repo' when calling AnnotationsApi->getLabelStudioUserAccess")

    projectId match {
      case Some(param) => queryParams += "project_id" -> param.toString
      case _ => queryParams
    }

    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def grantLabelStudioTeamAccess(body: GrantLabelStudioAccess,
    owner: String,
    repo: String,
    teamid: Long)(implicit reader: ClientResponseReader[Unit], writer: RequestWriter[GrantLabelStudioAccess]): Future[Unit] = {
    // create path and map variables
    val path = (addFmt("/repos/{owner}/{repo}/annotations/access/teams/{teamid}")
      replaceAll("\\{" + "owner" + "\\}", owner.toString)
      replaceAll("\\{" + "repo" + "\\}", repo.toString)
      replaceAll("\\{" + "teamid" + "\\}", teamid.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (body == null) throw new Exception("Missing required parameter 'body' when calling AnnotationsApi->grantLabelStudioTeamAccess")
    if (owner == null) throw new Exception("Missing required parameter 'owner' when calling AnnotationsApi->grantLabelStudioTeamAccess")

    if (repo == null) throw new Exception("Missing required parameter 'repo' when calling AnnotationsApi->grantLabelStudioTeamAccess")


    val resFuture = client.submit("PUT", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def grantLabelStudioUserAccess(body: GrantLabelStudioAccess,
    owner: String,
    repo: String,
    userid: Long)(implicit reader: ClientResponseReader[Unit], writer: RequestWriter[GrantLabelStudioAccess]): Future[Unit] = {
    // create path and map variables
    val path = (addFmt("/repos/{owner}/{repo}/annotations/access/users/{userid}")
      replaceAll("\\{" + "owner" + "\\}", owner.toString)
      replaceAll("\\{" + "repo" + "\\}", repo.toString)
      replaceAll("\\{" + "userid" + "\\}", userid.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (body == null) throw new Exception("Missing required parameter 'body' when calling AnnotationsApi->grantLabelStudioUserAccess")
    if (owner == null) throw new Exception("Missing required parameter 'owner' when calling AnnotationsApi->grantLabelStudioUserAccess")

    if (repo == null) throw new Exception("Missing required parameter 'repo' when calling AnnotationsApi->grantLabelStudioUserAccess")


    val resFuture = client.submit("PUT", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def listLabelStudioTeamAccess(owner: String,
    repo: String,
    projectId: Option[Long] = None
    )(implicit reader: ClientResponseReader[List[LabelStudioProjectAccess]]): Future[List[LabelStudioProjectAccess]] = {
    // create path and map variables
    val path = (addFmt("/repos/{owner}/{repo}/annotations/access/teams")
      replaceAll("\\{" + "owner" + "\\}", owner.toString)
      replaceAll("\\{" + "repo" + "\\}", repo.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (owner == null) throw new Exception("Missing required parameter 'owner' when calling AnnotationsApi->listLabelStudioTeamAccess")

    if (repo == null) throw new Exception("Missing required parameter 'repo' when calling AnnotationsApi->listLabelStudioTeamAccess")

    projectId match {
      case Some(param) => queryParams += "project_id" -> param.toString
      case _ => queryParams
    }

    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def listLabelStudioUserAccess(owner: String,
    repo: String,
    projectId: Option[Long] = None
    )(implicit reader: ClientResponseReader[List[LabelStudioProjectAccess]]): Future[List[LabelStudioProjectAccess]] = {
    // create path and map variables
    val path = (addFmt("/repos/{owner}/{repo}/annotations/access/users")
      replaceAll("\\{" + "owner" + "\\}", owner.toString)
      replaceAll("\\{" + "repo" + "\\}", repo.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (owner == null) throw new Exception("Missing required parameter 'owner' when calling AnnotationsApi->listLabelStudioUserAccess")

    if (repo == null) throw new Exception("Missing required parameter 'repo' when calling AnnotationsApi->listLabelStudioUserAccess")

    projectId match {
      case Some(param) => queryParams += "project_id" -> param.toString
      case _ => queryParams
    }

    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def revokeLabelStudioTeamAccess(owner: String,
    repo: String,
    teamid: Long,
    projectId: Option[Long] = None
    )(implicit reader: ClientResponseReader[Unit]): Future[Unit] = {
    // create path and map variables
    val path = (addFmt("/repos/{owner}/{repo}/annotations/access/teams/{teamid}")
      replaceAll("\\{" + "owner" + "\\}", owner.toString)
      replaceAll("\\{" + "repo" + "\\}", repo.toString)
      replaceAll("\\{" + "teamid" + "\\}", teamid.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (owner == null) throw new Exception("Missing required parameter 'owner' when calling AnnotationsApi->revokeLabelStudioTeamAccess")

    if (repo == null) throw new Exception("Missing required parameter 'repo' when calling AnnotationsApi->revokeLabelStudioTeamAccess")

    projectId match {
      case Some(param) => queryParams += "project_id" -> param.toString
      case _ => queryParams
    }

    val resFuture = client.submit("DELETE", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def revokeLabelStudioUserAccess(owner: String,
    repo: String,
    userid: Long,
    projectId: Option[Long] = None
    )(implicit reader: ClientResponseReader[Unit]): Future[Unit] = {
    // create path and map variables
    val path = (addFmt("/repos/{owner}/{repo}/annotations/access/users/{userid}")
      replaceAll("\\{" + "owner" + "\\}", owner.toString)
      replaceAll("\\{" + "repo" + "\\}", repo.toString)
      replaceAll("\\{" + "userid" + "\\}", userid.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (owner == null) throw new Exception("Missing required parameter 'owner' when calling AnnotationsApi->revokeLabelStudioUserAccess")

    if (repo == null) throw new Exception("Missing required parameter 'repo' when calling AnnotationsApi->revokeLabelStudioUserAccess")

    projectId match {
      case Some(param) => queryParams += "project_id" -> param.toString
      case _ => queryParams
    }

    val resFuture = client.submit("DELETE", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }


}
