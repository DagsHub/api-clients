# DagsHub API
#
# This API provides a way to retrive & interact with data about DagsHub repositories, users, issues, webhooks and more. The API is also used by the [DagsHub Direct Data Access streaming client](https://github.com/DagsHub/client) to stream content from a repository for easier and faster training. 
#
# OpenAPI spec version: 1.0.2
# 
# Generated by: https://github.com/swagger-api/swagger-codegen.git

#' ExperimentKeys Class
#'
#' @field experiment_keys 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ExperimentKeys <- R6::R6Class(
  'ExperimentKeys',
  public = list(
    `experiment_keys` = NULL,
    initialize = function(`experiment_keys`){
      if (!missing(`experiment_keys`)) {
        stopifnot(is.list(`experiment_keys`), length(`experiment_keys`) != 0)
        lapply(`experiment_keys`, function(x) stopifnot(is.character(x)))
        self$`experiment_keys` <- `experiment_keys`
      }
    },
    toJSON = function() {
      ExperimentKeysObject <- list()
      if (!is.null(self$`experiment_keys`)) {
        ExperimentKeysObject[['experiment_keys']] <- self$`experiment_keys`
      }

      ExperimentKeysObject
    },
    fromJSON = function(ExperimentKeysJson) {
      ExperimentKeysObject <- jsonlite::fromJSON(ExperimentKeysJson)
      if (!is.null(ExperimentKeysObject$`experiment_keys`)) {
        self$`experiment_keys` <- ExperimentKeysObject$`experiment_keys`
      }
    },
    toJSONString = function() {
       sprintf(
        '{
           "experiment_keys": [%s]
        }',
        lapply(self$`experiment_keys`, function(x) paste(paste0('"', x, '"'), sep=","))
      )
    },
    fromJSONString = function(ExperimentKeysJson) {
      ExperimentKeysObject <- jsonlite::fromJSON(ExperimentKeysJson)
      self$`experiment_keys` <- ExperimentKeysObject$`experiment_keys`
    }
  )
)
